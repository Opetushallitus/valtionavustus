name: Deploy dev
on:
  workflow_dispatch:
  push:
    branches:
      - green-master

defaults:
  run:
    shell: bash

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    secrets: inherit
    with:
      env: dev
