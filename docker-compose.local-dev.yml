name: valtionavustus-dev
services:
  hakija:
    volumes:
      - type: bind
        source: ./server/
        target: /app/server/
  virkailija:
    volumes:
      - type: bind
        source: ./server/
        target: /app/server/
